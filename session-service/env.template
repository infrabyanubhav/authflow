# =============================================================================
# AuthFlow Supabase - Session Service Environment Template
# =============================================================================
# This template file contains all the environment variables needed for the
# session service. Copy this file to .env.development, .env.production, etc.
# and fill in the actual values for your environment.
# 
# IMPORTANT: Never commit actual .env files to version control!
# Only commit this template file.
# =============================================================================

# =============================================================================
# Redis Configuration
# =============================================================================
# Redis connection settings for session storage and caching
REDIS_HOST=localhost          # Redis server hostname or IP address
PORT=6379                     # Redis server port (default: 6379)

# =============================================================================
# Service URLs
# =============================================================================
# URLs for communicating with other services in the AuthFlow ecosystem
AUTH_URL=http://auth-service:8000    # URL of the auth service for verification
BACKEND_URL=http://localhost:8000    # Backend service URL for internal communication

# =============================================================================
# API Configuration
# =============================================================================
# API routing and prefix settings
PREFIX=/api/v1                # API prefix for all session service endpoints

# =============================================================================
# Development/Production Settings
# =============================================================================
# Environment-specific settings (development/production/test)
NODE_ENV=development          # Environment mode (development/production/test)
DEBUG=true                    # Enable debug mode for development

# =============================================================================
# Security Settings
# =============================================================================
# Security-related environment variables
SESSION_SECRET=your-session-secret-key-here    # Secret key for session encryption
JWT_SECRET=your-jwt-secret-key-here            # Secret key for JWT tokens

# =============================================================================
# Logging Configuration
# =============================================================================
# Logging settings for the session service
LOG_LEVEL=INFO                # Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
LOG_FILE=session-service.log  # Log file name

# =============================================================================
# Performance Settings
# =============================================================================
# Performance and optimization settings
WORKER_PROCESSES=4            # Number of worker processes
MAX_CONNECTIONS=1000          # Maximum concurrent connections

# =============================================================================
# Timeout Settings
# =============================================================================
# Various timeout configurations
SESSION_TIMEOUT=3600          # Session timeout in seconds (1 hour)
REDIS_TIMEOUT=5               # Redis connection timeout in seconds
REQUEST_TIMEOUT=30            # HTTP request timeout in seconds

# =============================================================================
# Rate Limiting
# =============================================================================
# Rate limiting configuration
RATE_LIMIT_REQUESTS=100       # Maximum requests per time window
RATE_LIMIT_WINDOW=60          # Time window for rate limiting in seconds

# =============================================================================
# Monitoring and Health Checks
# =============================================================================
# Health check and monitoring settings
HEALTH_CHECK_INTERVAL=30      # Health check interval in seconds
METRICS_ENABLED=true          # Enable metrics collection

# =============================================================================
# Database Settings (if using additional databases)
# =============================================================================
# Database connection settings (if needed beyond Redis)
DB_HOST=localhost             # Database host
DB_PORT=5432                  # Database port
DB_NAME=session_db            # Database name
DB_USER=session_user          # Database username
DB_PASSWORD=your-db-password  # Database password

# =============================================================================
# External Service Integration
# =============================================================================
# Settings for external service integrations
SUPABASE_URL=your-supabase-url    # Supabase project URL
SUPABASE_KEY=your-supabase-key     # Supabase service role key

# =============================================================================
# Email Settings (if email functionality is added)
# =============================================================================
# Email service configuration (for future features)
SMTP_HOST=smtp.gmail.com      # SMTP server host
SMTP_PORT=587                 # SMTP server port
SMTP_USER=your-email@gmail.com # SMTP username
SMTP_PASSWORD=your-app-password # SMTP password

# =============================================================================
# CORS Settings
# =============================================================================
# Cross-Origin Resource Sharing configuration
CORS_ORIGINS=http://localhost:3000,http://localhost:8000  # Allowed CORS origins
CORS_METHODS=GET,POST,PUT,DELETE,OPTIONS  # Allowed HTTP methods
CORS_HEADERS=Content-Type,Authorization   # Allowed headers

# =============================================================================
# SSL/TLS Settings (for production)
# =============================================================================
# SSL/TLS configuration for secure connections
SSL_ENABLED=false             # Enable SSL/TLS (set to true in production)
SSL_CERT_PATH=/path/to/cert.pem    # SSL certificate path
SSL_KEY_PATH=/path/to/key.pem      # SSL private key path

# =============================================================================
# Backup and Recovery
# =============================================================================
# Backup and recovery settings
BACKUP_ENABLED=false          # Enable automatic backups
BACKUP_INTERVAL=86400         # Backup interval in seconds (24 hours)
BACKUP_PATH=/backups          # Backup storage path

# =============================================================================
# Notes for Developers
# =============================================================================
# 
# 1. Copy this file to .env.development for development environment
# 2. Copy this file to .env.production for production environment
# 3. Replace placeholder values with actual configuration
# 4. Never commit actual .env files to version control
# 5. Use strong, unique secrets for production environments
# 6. Regularly rotate secrets and keys
# 7. Use environment-specific values for different deployments
#
# Example usage:
# cp env.template .env.development
# cp env.template .env.production
# Then edit the files with actual values
# =============================================================================
